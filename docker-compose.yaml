version: "3.9"
services:
  web:
    build: .
    # For local testing; Coolify will map ports automatically in its UI
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
